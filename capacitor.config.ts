import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.nutritioncalculatorappsetup',
  appName: 'nutrition-calculator-app-setup',
  webDir: 'dist'
};

export default config;
